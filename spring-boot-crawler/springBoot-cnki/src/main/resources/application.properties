# 设置端口
server.port=8080

# 设置数据库
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://47.98.50.37:5432/dbtest
spring.datasource.username=ydj
spring.datasource.password=ydjws

# mybatis配置
mybatis.mapper-locations=classpath:mapping/*Mapper.xml
mybatis.type-aliases-package=com.ifind.entity

# 动态配置
webChromDriver=D:/chromDriver/chromedriver.exe